plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android") version "1.5.30"
}

android {
    compileSdkVersion(31)
    defaultConfig {
        applicationId = "dev.klewis.hellokotlin"
	targetSdkVersion(31)
    }
}
